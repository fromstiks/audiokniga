<?xml version="1.0" encoding="utf-8"?>
<!--
  Виджет собран на RemoteViews: Compose в нём не работает. Палитра взята светлая
  неоморфная — виджет лежит на обоях, и подстроиться под них всё равно нельзя.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#2B2924"
        android:textSize="14sp"
        android:textStyle="bold"
        android:text="@string/app_name" />

    <TextView
        android:id="@+id/widget_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#726D62"
        android:textSize="11sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"
        android:gravity="center">

        <ImageButton
            android:id="@+id/widget_rewind"
            android:layout_width="46dp"
            android:layout_height="46dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:scaleType="fitCenter"
            android:padding="11dp"
            android:src="@drawable/ic_media_rewind"
            android:contentDescription="@string/widget_rewind" />

        <ImageButton
            android:id="@+id/widget_play"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:scaleType="fitCenter"
            android:padding="13dp"
            android:src="@drawable/ic_media_play"
            android:contentDescription="@string/widget_play" />

        <ImageButton
            android:id="@+id/widget_forward"
            android:layout_width="46dp"
            android:layout_height="46dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:scaleType="fitCenter"
            android:padding="11dp"
            android:src="@drawable/ic_media_forward"
            android:contentDescription="@string/widget_forward" />
    </LinearLayout>
</LinearLayout>
